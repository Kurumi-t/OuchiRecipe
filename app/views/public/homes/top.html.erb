<div class="container mt-3 mb-5">
  <div class="row">
    <div class="col-12 col-md-8 mx-auto">
      <div class="top-bg">
        <div class="top-bg-content">
          <p class="h3 text-center">~while staying to home~</p>
          <p class="text-center pt-3">おうちにある食材からレシピを検索する</p>
        </div>
      </div>
    </div>
    <div class="col-12 col-md-8 mx-auto pt-5">
      <h3 class="text-center">おすすめレシピ</h3>
      <div class="row text-center pt-3">
        <% @all_ranks.each do |post_recipe| %>
          <div class="col-6 col-sm-3">
            <%= link_to(post_recipe_path(post_recipe.id), class: "text-dark") do %>
              <%= image_tag post_recipe.get_recipe_image(270,150), class:"img-fluid rounded" %>
              <p>
                <%= post_recipe.title %><br>
            <% end %>
                <%= image_tag post_recipe.user.get_profile_image(35,35),class:"rounded-circle" %>
                <%= post_recipe.user.name %>
              </p>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>